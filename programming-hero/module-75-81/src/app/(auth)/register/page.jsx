/*
"If a folder is created for organizing files and does not need to be set in the route path, it should be declared inside parentheses()."
*/

const Register = () => {
  return (
    <div>
      <h2 className="text-3xl">This is Register Page</h2>
    </div>
  );
};

export default Register;
