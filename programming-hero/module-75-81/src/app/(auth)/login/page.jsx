/*
If a folder is created for organizing files and does not need to be set in the route path, it should be declared inside parentheses(). You can learn more about this in the Next.js 15 documentation under named 'Route Groups' (without affecting the routing).
*/

const Login = () => {
  return (
    <div>
      <h2 className="text-3xl">This is Login Page</h2>
    </div>
  );
};

export default Login;
