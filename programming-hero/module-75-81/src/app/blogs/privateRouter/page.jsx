import React from 'react';

const PrivateRouter = () => {
    return (
        <div>
            <h2 className="text-3xl italic font-semibold">Only those who are logged in have access to this private router</h2>
        </div>
    );
};

export default PrivateRouter;