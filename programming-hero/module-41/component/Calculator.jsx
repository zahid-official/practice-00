const add = (first, second)=>{
    return first + second;
}

const subtract = (first, second)=>{
    return first - second;
}

const multiply = (first, second)=>{
    return first * second;
}

const testAliasToDivition = (first, second)=>{
    return first / second;
}

export {add, subtract, multiply, testAliasToDivition}
